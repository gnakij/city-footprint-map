

### 顶栏主题色最终值确定：所有主题统一 8%（2026-06-20）

修复了"Linear/Stripe主题被高优先级.glass规则覆盖"这个重大bug后，用户
重新看到了真实生效的紫色效果，确认把Stripe也调整为8%——现在Rose/Linear/
Stripe三个主题的`--topbar-tint-pct`都是8%，完全统一。Stripe的覆盖声明
（189行附近）虽然现在数值跟默认值一样、技术上可以省略，但保留这条覆盖
方便以后如果只想单独调整某个主题时直接改这一行，不需要重新拆分变量结构。

已用curl直接拉取线上CSS确认两处定义都是8%生效。

**遗留事项**：之前测试80%时算过的文字对比度问题（深色Logo/用户名文字在
高比例混色下对比度不足）——8%这个最终比例下重新测算对比度应该是安全的
（远低于之前算过的80%临界值），但若用户后续反馈顶栏文字看不清，需要回头
检查`--color-on-surface`跟混色后背景的对比度。

已验证：`npx tsc --noEmit`/`npm run build`通过，已部署并用curl确认线上生效。
